{
  "mcpServers": {
    "docker-demo": {
      "command": "docker",
      "args": [
        "run",
        "--rm",
        "--privileged",
        "-v",
        "/var/run/docker.sock:/var/run/docker.sock",
        "example/mcp:latest"
      ]
    }
  }
}
